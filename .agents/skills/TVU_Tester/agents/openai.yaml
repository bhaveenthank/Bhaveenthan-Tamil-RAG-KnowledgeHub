interface:
  display_name: "TVU Tester"
  short_description: "Validate extraction and regressions."
  brand_color: "#CA8A04"
  default_prompt: "Use $TVU_Tester to design and run validation for the current TamilVU change."
policy:
  allow_implicit_invocation: true

